# 版本记录

本文记录 Autosign 各版本的主要变化。

## v1.1.9-beta

这是 1.1.9 beta 测试版本，先修复一个待办页场景。

- 当待办页找不到“电子签章”入口时，脚本会认为当前没有电子签章待办，并自动停止运行。
- 状态提示改为“没有电子签章待办，已停止”，避免用户误以为脚本卡住或需要刷新页面。
- 这个处理方式和批量签章页没有待签文件时保持一致。

## v1.1.8

这是 1.1.8 正式版本，主要提升稳定性、可观察性和首次使用体验。

- 优化批量签章列表刷新逻辑。后续批次不再简单按固定总数判断，而是按待签章数量变化判断列表是否已经稳定，减少重复勾选同一批文件的情况。
- 新增“查询间隔”设置，默认 3 秒。网络或电脑较慢时，可以在设置里适当调大，让列表刷新判断更稳。
- 优化签字页切换文件逻辑。签完当前文件后，切换下一份文件时不再固定等待，而是等页面、画布和签名模块准备好后再继续。
- 修复切换文件后页面已加载但脚本没有继续识别的问题，兼容 PMS 页面里的选中文件图标。
- 优化首次进入电子签章流程的等待方式，减少因为页面反应慢导致的误判。
- 优化运行、停止和手动重启体验。手动停止后再次点击运行可以重新开始，不需要反复刷新页面。
- 自动停止时不再强制刷新业务页面，减少页面被打断的情况。
- 状态提示更清楚。运行时可以看到脚本正在查找入口、刷新列表、勾选文件、切换文件或等待页面加载。
- 设置面板更易用，支持点击浮窗外关闭，并增加查询间隔设置和日志导出入口。
- 新增日志导出功能。遇到问题时可以导出日志，方便结合用户描述判断卡在哪一步。
- 日志导出会隐藏常见敏感链接参数，并改善 Windows 下打开日志时的中文显示。
- 补充首次使用说明，提示 Tampermonkey 授权、浏览器弹窗权限等常见问题。

## v1.1.7

- 支持在移动公司内网环境使用，分公司账号也可以进入签章流程。
- 优化批量签章列表刷新逻辑。后续批次会定时查询列表，等已经签过的文件不再显示后，再继续勾选下一批。
- 修复 1.1.5 中签完第一批后无法继续处理下一批的问题。

## v1.1.6

- 新增 `cpms.hq.cmcc` 和通用签章页匹配范围，兼容更多签章入口。
- 优化签字位置和流程运行状态处理。
- 改善待办、批量签章和签名页之间的流程衔接。

## v1.1.5

- 恢复站点级脚本匹配范围，避免前端路由或中间页面导致脚本无法注入。
- 新增页面类型判断，只在待办列表页、批量签章页和单个签名页显示控制按钮。

## v1.1.4

- 脚本名称改为 `PMS系统自动签章助手`。
- 调整脚本描述和作者信息。

## v1.1.3

- 限制脚本只在自动化流程实际生效的页面中加载。
- 适用页面包括待办列表页、批量签章页和单个签名页。

## v1.1.2

- 新增可配置的签字位置选项。
- 新增设置按钮和签字位置选择器。
- 优化列表刷新后的“处理”按钮识别逻辑。
- 优化签章运行过程中的状态提示。

## v1.1.1

- 新增实时状态栏更新。
- 优化签名流程和页面状态反馈。
- 优化画布和签名模块的等待策略。
- 优化长流程运行和停止状态处理。

## v1.1.0

- 原始版本号为 `0.4`。
- 基于状态元素选择重构处理流程。
- 减少脚本体积，并简化自动签章路径。

## v1.0.1

- 原始版本号为 `0.3`。
- 修复元素选择器处理逻辑。
- 继续使用统一主脚本文件名 `autoSign.js`，便于维护。

## v1.0

- 初始归档版本，原始版本号为 `0.2`。
- 新增 Tampermonkey 元数据。
- 新增运行/停止控制按钮。
- 新增运行状态持久化。
- 新增自动签章流程和基础重试处理。
